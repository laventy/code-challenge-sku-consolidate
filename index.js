const getConsolidatedCatelog = () => {
  const result = [];

  // Put all catelog items within A into result
  // ...

  // Go through all catelog items one by one within B to compare with each item of A
  // Ignore it if a duplicate. otherwise, add it into final result
  // if B1 a duplicate of A1? => isDuplicate(A1, B1)
};

getConsolidatedCatelog();
